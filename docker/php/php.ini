memory_limit=512M
upload_max_filesize=64M
post_max_size=64M
max_execution_time=120

; Good defaults for local dev
display_errors=On
error_reporting=E_ALL

; Opcache is fine in dev, but keep it modest
opcache.enable=1
opcache.validate_timestamps=1
opcache.revalidate_freq=0
